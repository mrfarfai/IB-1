# Инструкция по настройке проекта

## Быстрый старт

1. **Инициализация Git репозитория:**
```bash
git init
git add .
git commit -m "Initial commit: Secure backend API with OWASP protection"
```

2. **Связывание с удаленным репозиторием GitHub:**
```bash
# Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

3. **Установка зависимостей:**
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

4. **Запуск приложения:**
```bash
python app.py
```

5. **Тестирование API:**
```bash
# Вариант 1: Python скрипт
python test_api.py

# Вариант 2: Bash скрипт
./test_api.sh

# Вариант 3: Вручную с curl
curl -X POST http://localhost:5001/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "testpass123"}'
```

## Проверка безопасности

### Локальный запуск сканеров:

```bash
# Bandit (SAST) - ВАЖНО: исключайте venv!
bandit -r . -x venv -x .venv -x env
# Или сканируйте только ваши файлы:
bandit app.py test_api.py

# Safety (SCA) - требует регистрации
safety scan
# Или используйте pip-audit как альтернативу:
pip-audit

# pip-audit
pip-audit
```

### GitHub Actions

После push в репозиторий, GitHub Actions автоматически запустит:
- Bandit для статического анализа
- Safety для проверки зависимостей
- pip-audit для дополнительной проверки

Отчеты будут доступны в разделе Actions вашего репозитория.

## Важные замечания

⚠️ **Безопасность:**
- В продакшене обязательно измените `JWT_SECRET_KEY` в `app.py`
- Используйте переменные окружения для хранения секретов
- Не коммитьте файлы `.env` с реальными секретами

📝 **Тестовые данные:**
- Username: `testuser`
- Password: `testpass123`

