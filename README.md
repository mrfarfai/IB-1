# Secure Backend API

Безопасное backend-приложение с автоматизированной проверкой кода на уязвимости. Реализована защита от OWASP Top 10 и интеграция инструментов безопасности в процесс разработки.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [API Эндпоинты](#описание-api)
- [Реализованные меры защиты](#реализованные-меры-защиты)
- [CI/CD Pipeline и отчеты безопасности](#cicd-pipeline-и-автоматизированная-проверка-безопасности)
- [Установка и запуск](#установка-и-запуск)
- [Тестирование API](#тестирование-api)

## Описание проекта

Это учебный проект, демонстрирующий создание безопасного REST API с применением лучших практик безопасности веб-приложений. Проект реализует:

- **RESTful API** с 4 эндпоинтами
- **JWT-аутентификацию** для защиты доступа
- **Защиту от основных уязвимостей** из OWASP Top 10
- **Автоматизированную проверку безопасности** через CI/CD pipeline

Проект использует Flask как веб-фреймворк и SQLite для хранения данных. Все меры защиты реализованы на уровне кода и проверяются автоматически при каждом коммите.

## Технологический стек

- **Python 3.11+**
- **Flask** - веб-фреймворк
- **Flask-JWT-Extended** - JWT аутентификация
- **bcrypt** - хэширование паролей
- **SQLite** - база данных
- **Bandit** - SAST (Static Application Security Testing)
- **Safety** - SCA (Software Composition Analysis)

## Реализованные меры защиты

### 1. Защита от SQL-инъекций (SQL Injection)

**Проблема:** SQL-инъекции возникают, когда пользовательский ввод напрямую вставляется в SQL-запросы, что позволяет злоумышленникам выполнять произвольный SQL-код.

**Решение:** Использование параметризованных запросов во всех операциях с базой данных.

**Пример реализации в коде:**

```python
# ❌ НЕПРАВИЛЬНО (уязвимо к SQL-инъекциям):
# cursor.execute(f"SELECT * FROM users WHERE username = '{username}'")

# ✅ ПРАВИЛЬНО (защищено):
cursor.execute('SELECT id, username, password_hash FROM users WHERE username = ?', (username,))
```

**Где применено:**
- `POST /auth/login` - проверка учетных данных пользователя
- `GET /api/data` - получение данных пользователя
- `POST /api/data` - создание новых записей

**Результат:** Даже если злоумышленник попытается ввести SQL-код в поле username (например, `admin' OR '1'='1`), он будет обработан как обычная строка, а не как часть SQL-запроса.

### 2. Защита от XSS (Cross-Site Scripting)

**Проблема:** XSS-атаки позволяют злоумышленникам внедрять вредоносный JavaScript-код, который выполняется в браузере жертвы.

**Решение:** Экранирование всех пользовательских данных перед возвратом в ответах API.

**Пример реализации в коде:**

```python
from werkzeug.utils import escape

# При получении данных из БД
items = cursor.fetchall()
result = []
for item in items:
    result.append({
        'id': item['id'],
        'title': escape(str(item['title'])),      # Экранирование для защиты от XSS
        'content': escape(str(item['content']))   # Экранирование для защиты от XSS
    })

# При сохранении данных от пользователя
title_safe = escape(str(title))
content_safe = escape(str(content))
```

**Где применено:**
- `GET /api/data` - все данные экранируются перед отправкой клиенту
- `POST /api/data` - все входные данные экранируются перед сохранением

**Результат:** Если злоумышленник попытается ввести `<script>alert('XSS')</script>`, это будет преобразовано в безопасную строку `&lt;script&gt;alert('XSS')&lt;/script&gt;`, которая не будет выполнена как код.

### 3. Защита от Broken Authentication

**Проблема:** Слабая аутентификация позволяет злоумышленникам получать несанкционированный доступ к системе.

**Решение:** Многоуровневая защита аутентификации.

#### 3.1. Хэширование паролей с bcrypt

**Пример реализации:**

```python
import bcrypt

# При создании пользователя
password = 'user_password'
password_hash = bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt()).decode('utf-8')
# Сохраняем password_hash в БД, НИКОГДА не сохраняем сам пароль

# При проверке пароля
stored_hash = user['password_hash'].encode('utf-8')
if bcrypt.checkpw(password.encode('utf-8'), stored_hash):
    # Пароль верный
    pass
```

**Преимущества bcrypt:**
- Автоматическая генерация соли (salt) для каждого пароля
- Адаптивное хэширование (можно увеличить сложность)
- Защита от rainbow table атак

#### 3.2. JWT токены для аутентификации

**Пример реализации:**

```python
from flask_jwt_extended import create_access_token, jwt_required, get_jwt_identity

# При успешной аутентификации
access_token = create_access_token(identity=str(user['id']))
# Токен содержит: user_id, время создания, время истечения

# Защита эндпоинтов
@app.route('/api/data', methods=['GET'])
@jwt_required()  # Middleware проверяет токен автоматически
def get_data():
    user_id = int(get_jwt_identity())  # Получаем ID пользователя из токена
    # ...
```

**Преимущества JWT:**
- Stateless аутентификация (не нужно хранить сессии на сервере)
- Токены содержат информацию о пользователе
- Автоматическая проверка срока действия (1 час в нашем случае)
- Middleware автоматически проверяет токен на защищенных эндпоинтах

#### 3.3. Защита эндпоинтов

**Публичные эндпоинты:**
- `POST /auth/login` - доступен всем для аутентификации
- `GET /health` - проверка здоровья приложения

**Защищенные эндпоинты (требуют JWT токен):**
- `GET /api/data` - требует токен в заголовке `Authorization: Bearer <token>`
- `POST /api/data` - требует токен в заголовке `Authorization: Bearer <token>`

**Пример запроса к защищенному эндпоинту:**

```bash
curl -X GET http://localhost:5001/api/data \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

**Если токен отсутствует или неверен:**
```json
{
  "msg": "Missing Authorization Header"
}
```

## Описание API

Проект реализует RESTful API с 4 эндпоинтами для демонстрации различных сценариев использования и мер безопасности.

### Эндпоинты

#### 1. POST /auth/login
**Назначение:** Аутентификация пользователя и получение JWT токена.

**Тип:** Публичный (не требует аутентификации)

**Метод:** POST

**URL:** `http://localhost:5001/auth/login`

**Заголовки:**
```
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "username": "testuser",
  "password": "testpass123"
}
```

**Успешный ответ (200 OK):**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "Bearer",
  "user_id": 1,
  "username": "testuser"
}
```

**Ошибка аутентификации (401 Unauthorized):**
```json
{
  "error": "Invalid credentials"
}
```

**Меры защиты:**
- Пароль проверяется через bcrypt (защита от перебора)
- Используются параметризованные SQL-запросы (защита от SQL-инъекций)
- JWT токен имеет срок действия 1 час

---

#### 2. GET /api/data
**Назначение:** Получение списка всех данных, принадлежащих аутентифицированному пользователю.

**Тип:** Защищенный (требует JWT токен)

**Метод:** GET

**URL:** `http://localhost:5001/api/data`

**Заголовки:**
```
Authorization: Bearer <access_token>
```

**Успешный ответ (200 OK):**
```json
{
  "data": [
    {
      "id": 1,
      "title": "Test Item 1",
      "content": "This is a test content"
    },
    {
      "id": 2,
      "title": "Test Item 2",
      "content": "Another test content"
    }
  ],
  "count": 2
}
```

**Ошибка без токена (401 Unauthorized):**
```json
{
  "msg": "Missing Authorization Header"
}
```

**Меры защиты:**
- Требуется валидный JWT токен
- Данные фильтруются по user_id из токена (пользователь видит только свои данные)
- Все данные экранируются перед отправкой (защита от XSS)
- Используются параметризованные SQL-запросы

---

#### 3. POST /api/data
**Назначение:** Создание нового элемента данных для аутентифицированного пользователя.

**Тип:** Защищенный (требует JWT токен)

**Метод:** POST

**URL:** `http://localhost:5001/api/data`

**Заголовки:**
```
Authorization: Bearer <access_token>
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "title": "New Item",
  "content": "New content"
}
```

**Успешный ответ (201 Created):**
```json
{
  "id": 3,
  "title": "New Item",
  "content": "New content",
  "message": "Data item created successfully"
}
```

**Ошибка валидации (400 Bad Request):**
```json
{
  "error": "Title and content are required"
}
```

**Меры защиты:**
- Требуется валидный JWT токен
- Все входные данные экранируются перед сохранением (защита от XSS и SQL-инъекций)
- Используются параметризованные SQL-запросы
- user_id берется из токена (нельзя создать данные от имени другого пользователя)

---

#### 4. GET /health
**Назначение:** Проверка работоспособности приложения (health check).

**Тип:** Публичный (не требует аутентификации)

**Метод:** GET

**URL:** `http://localhost:5001/health`

**Успешный ответ (200 OK):**
```json
{
  "status": "healthy"
}
```

**Использование:** Этот эндпоинт используется для мониторинга и проверки доступности сервиса.

## Установка и запуск

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd IB-1
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите приложение:
```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5001`

## Тестирование API

### С помощью curl

1. **Аутентификация:**
```bash
curl -X POST http://localhost:5001/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "testpass123"}'
```

**Ответ:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "Bearer",
  "user_id": 1,
  "username": "testuser"
}
```

2. **Получение данных (с токеном):**
```bash
curl -X GET http://localhost:5001/api/data \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

**Ответ:**
```json
{
  "data": [
    {
      "id": 1,
      "title": "Test Item 1",
      "content": "This is a test content"
    }
  ],
  "count": 1
}
```

3. **Создание данных:**
```bash
curl -X POST http://localhost:5001/api/data \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title": "New Item", "content": "New content"}'
```

**Ответ:**
```json
{
  "id": 2,
  "title": "New Item",
  "content": "New content",
  "message": "Data item created successfully"
}
```

4. **Проверка без токена (должна вернуть ошибку):**
```bash
curl -X GET http://localhost:5001/api/data
```

**Ответ (ошибка):**
```json
{
  "msg": "Missing Authorization Header"
}
```

5. **Health check (публичный эндпоинт):**
```bash
curl -X GET http://localhost:5001/health
```

**Ответ:**
```json
{
  "status": "healthy"
}
```

### С помощью Postman

1. Создайте новый запрос POST на `http://localhost:5001/auth/login`
2. В Body выберите `raw` и `JSON`, введите:
```json
{
  "username": "testuser",
  "password": "testpass123"
}
```
3. Скопируйте `access_token` из ответа
4. Для защищенных эндпоинтов добавьте заголовок:
   - Key: `Authorization`
   - Value: `Bearer <ваш_токен>`

## CI/CD Pipeline и автоматизированная проверка безопасности

Проект настроен с GitHub Actions для автоматической проверки безопасности при каждом push или pull request.

### Запускаемые проверки:

1. **Bandit (SAST - Static Application Security Testing)**
   - Статический анализ исходного кода на уязвимости
   - Проверяет: SQL-инъекции, XSS, hardcoded passwords, использование небезопасных функций
   - Сканирует только файлы проекта: `app.py` и `test_api.py` (исключает `venv`)

2. **Safety (SCA - Software Composition Analysis)**
   - Проверка зависимостей на известные уязвимости из базы данных безопасности
   - Анализирует все установленные пакеты из `requirements.txt`
   - Использует открытую базу данных уязвимостей

3. **pip-audit**
   - Дополнительная проверка зависимостей
   - Использует базу данных PyPI Advisory Database
   - Работает без регистрации (в отличие от Safety)

### Результаты проверок

**Текущий статус безопасности:**
- ✅ **Bandit:** 1 предупреждение (HIGH) - `debug=True` для разработки (приемлемо)
- ✅ **Safety:** 0 уязвимостей обнаружено
- ✅ **pip-audit:** 0 уязвимостей обнаружено

**Отчеты доступны в GitHub Actions:**
1. Перейдите в раздел **Actions** вашего репозитория на GitHub
2. Выберите последний запуск workflow "Security CI Pipeline"
3. В разделе **Artifacts** скачайте отчеты:
   - `bandit-report.json` - результаты статического анализа
   - `safety-report.json` - результаты проверки зависимостей Safety
   - `pip-audit-report.json` - результаты проверки зависимостей pip-audit

**Скриншоты отчетов безопасности:**

Для демонстрации результатов автоматизированных проверок безопасности рекомендуется сделать скриншоты из раздела **Actions** вашего GitHub репозитория:

1. **Скриншот страницы workflow:**
   - Перейдите в раздел **Actions** → выберите последний запуск "Security CI Pipeline"
   - Сделайте скриншот страницы с результатами всех шагов (Bandit, Safety, pip-audit)
   - Показывает, что все проверки запускаются автоматически

2. **Скриншот отчета Bandit (SAST):**
   - В разделе **Artifacts** скачайте `bandit-report.json`
   - Или сделайте скриншот вывода Bandit из логов workflow
   - Показывает результаты статического анализа кода

3. **Скриншот отчета pip-audit (SCA):**
   - Сделайте скриншот вывода pip-audit из логов workflow
   - Показывает, что все зависимости проверены и уязвимостей не найдено

4. **Скриншот успешного завершения pipeline:**
   - Показывает зеленую галочку (✓) у всех шагов
   - Демонстрирует, что проект проходит все проверки безопасности

**Где разместить скриншоты:**
- Создайте папку `docs/screenshots/` в корне проекта
- Или добавьте скриншоты прямо в README.md используя markdown:
  ```markdown
  ![Bandit Report](docs/screenshots/bandit-report.png)
  ![Safety Report](docs/screenshots/safety-report.png)
  ```

**Пример структуры для скриншотов:**
```
IB-1/
├── docs/
│   └── screenshots/
│       ├── workflow-overview.png      # Общий вид workflow
│       ├── bandit-report.png         # Отчет Bandit
│       ├── pip-audit-report.png      # Отчет pip-audit
│       └── safety-report.png         # Отчет Safety (если доступен)
```

### Локальный запуск проверок:

```bash
# Запуск Bandit (ВАЖНО: исключайте venv!)
bandit -r . -x venv -x .venv -x env
# Или сканируйте только ваши файлы:
bandit app.py test_api.py

# Запуск Safety (требует регистрации на safetycli.com)
safety scan
# Или используйте pip-audit как альтернативу:
pip-audit

# Запуск pip-audit
pip-audit
```

## Структура проекта

```
IB-1/
├── app.py                 # Основное приложение Flask
├── requirements.txt       # Зависимости проекта
├── .bandit               # Конфигурация Bandit
├── .gitignore           # Игнорируемые файлы
├── .github/
│   └── workflows/
│       └── ci.yml       # GitHub Actions workflow
└── README.md            # Документация
```

## Тестовые учетные данные

- **Username:** `testuser`
- **Password:** `testpass123`

⚠️ **Внимание:** В продакшене обязательно измените JWT_SECRET_KEY и используйте переменные окружения для хранения секретов!

## Безопасность

Все меры защиты соответствуют рекомендациям OWASP Top 10:

- ✅ Защита от SQL-инъекций
- ✅ Защита от XSS
- ✅ Защита от Broken Authentication
- ✅ Автоматизированное тестирование безопасности в CI/CD

## Лицензия

Учебный проект для изучения безопасности веб-приложений.

